      <div class="Content">
          <section class="personalSection">
              <header class="personalHeader" id="profileHeader">
                  <img src="" class="" alt="avatar">
                  <h1 class="">{{name}}</h1>
                  <h2 class="">{{city}}</h2>
                  <h2 class="">{{website}}</h2>
                  <h2 class="">{{email}}</h2>
              </header>
              <div class="images-profile">
                  {{#each pictures}}
                  <img src="{{this.data.image}}" class="image" alt="">
                  {{else}}
                  <p class="f5 f4-ns fw6 dark-gray">There aren't pictures yet!</p>
                  {{/each}}
              </div>
          </section>
      </div>

